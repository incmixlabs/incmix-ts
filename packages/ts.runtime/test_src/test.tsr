export type Type1 = { a: number; b: string };

export type Type2 = string | 4 | 2 | boolean;

export type Type3 = (a: number, b: string) => boolean;

export type Type4 = {
  a: 4 | 5 | string;
  b: Promise<string | number>;
};

export type Type5 = (a: number, b: string) => Promise<boolean>;

export type Type6<A, B, C, Type> = {
  a: A;
  b: B;
  c: Promise<C> | number;
  d: (a: number) => boolean;
};

export type ttType = <A, B>(a: A, b: B) => { a: A; b: B };
